<Window x:Class="Home.Packaging.ExtractionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Properties="clr-namespace:Home.Packaging.Properties"
        Width="400" Height="220" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="Transparent"
        SourceInitialized="WindowSourceInitialized">
    <Window.Resources>
        <LinearGradientBrush x:Key="TabControlBgBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#fbfcfd" Offset="0"/>
            <GradientStop Color="#e0e3e7" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Grid>
        <Rectangle VerticalAlignment="Top" Height="32">
            <Rectangle.OpacityMask>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00000000" Offset="0"/>
                    <GradientStop Color="#FF000000" Offset="0.05"/>
                    <GradientStop Color="#FF000000" Offset="0.95"/>
                    <GradientStop Color="#00000000" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.OpacityMask>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#00FFFFFF" Offset="0"/>
                    <GradientStop Color="#99FFFFFF" Offset="0.5"/>
                    <GradientStop Color="#FFFFFFFF" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        <!--<TextBlock Text="{x:Static Properties:Resources.Installing}" FontSize="14" FontWeight="Light" Margin="20,10,0,0"/>-->

        <DockPanel Name="Root" Margin="0,0,0,0" Background="{StaticResource TabControlBgBrush}">
            <Grid DockPanel.Dock="Bottom">
                <Rectangle Fill="#c1c2c3" VerticalAlignment="Top" Height="1" Margin="0,0,0,0"/>
                <Rectangle VerticalAlignment="Top" Height="4" Margin="0,1,0,0">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#AAc6c9cd" Offset="0"/>
                            <GradientStop Color="#e3e7ec" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <Button Name="CloseButton" Height="23" Content="{x:Static Properties:Resources.Close}" Margin="0,10,7,10" Padding="15,0,15,0" HorizontalAlignment="Right" IsEnabled="False" Click="CloseButtonClick"/>
            </Grid>
            <Grid Background="White">
                <StackPanel Margin="10">
                    <TextBlock Name="HeaderTextBlock" Text="{x:Static Properties:Resources.Extracting}" FontSize="24" FontWeight="Light" Foreground="#083ca2"/>
                    <Separator Margin="0,0,0,10" VerticalAlignment="Top"/>
                    <ProgressBar Name="ProgressBar" Height="18" Margin="0,0,0,5" Background="Transparent"/>
                    <TextBlock Name="CurrentFileTextBlock" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
