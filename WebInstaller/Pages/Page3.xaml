<UserControl x:Class="WebInstaller.Pages.Page3"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="420" Loaded="UserControl_Loaded">
    <Grid>
        <Grid Margin="0,0,0,40">
            <Rectangle Fill="#8b9097" VerticalAlignment="Bottom" Height="1" Margin="0,0,0,4"/>
            <Image Source="../Resources/bg.png" Stretch="UniformToFill" HorizontalAlignment="Center"/>
            <Rectangle VerticalAlignment="Bottom" Height="4">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#c6c9cd" Offset="0"/>
                        <GradientStop Color="#e3e7ec" Offset="1.0"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <StackPanel VerticalAlignment="Bottom" Margin="10">
                <TextBlock Name="StatusTextBlock" Text="{DynamicResource Connecting}"/>
                <ProgressBar Name="DownloadProgress" Height="15" IsIndeterminate="True" Background="Transparent"/>
            </StackPanel>
        </Grid>
        <Button Name="CancelButton" Content="{DynamicResource Cancel}" Width="100" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,10,10" Click="CancelButton_Click"/>
    </Grid>
</UserControl>
