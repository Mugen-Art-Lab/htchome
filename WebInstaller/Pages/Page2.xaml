<UserControl x:Class="WebInstaller.Pages.Page2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="420"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid Margin="0,0,0,40" Background="White">
            <Rectangle Fill="#8b9097" VerticalAlignment="Bottom" Height="1" Margin="0,0,0,4"/>
            <Rectangle VerticalAlignment="Bottom" Height="4">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#c6c9cd" Offset="0"/>
                        <GradientStop Color="#e3e7ec" Offset="1.0"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <StackPanel Orientation="Vertical" Margin="20">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Left" Text="{DynamicResource InstallPath}" VerticalAlignment="Center"/>
                    <Button Name="ChangeButton" DockPanel.Dock="Right" Content="{DynamicResource Change}" Margin="5,0,5,0" Width="100" Click="ChangeButton_Click"/>
                    <TextBox Name="PathTextBox" Margin="5,0,0,0" HorizontalAlignment="Stretch" Text="C:\Program Files\HTC Home" TextChanged="PathTextBox_TextChanged"/>
                </DockPanel>
                <Separator Margin="0,10,0,10"/>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Left" Text="{DynamicResource Shortcuts}"/>
                    <StackPanel Orientation="Vertical" Margin="30,0,0,0">
                        <CheckBox Name="DesktopShortcut" Content="{DynamicResource Desktop}" Click="DesktopShortcut_Click"/>
                        <CheckBox Name="StartMenuShortcut" Content="{DynamicResource StartMenu}" Margin="0,10,0,0" IsChecked="True" Click="StartMenuShortcut_Click"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Grid>
        <Button Name="InstallButton" Width="100" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,120,10" Click="InstallButton_Click">
            <DockPanel>
                <Image Name="ShieldIcon" Source="../Resources/shield.png" Width="16" Height="16" Margin="0,0,3,0" />
                <TextBlock Text="{DynamicResource Install}"/>
            </DockPanel>
        </Button>
        <Button Name="CancelButton" Content="{DynamicResource Cancel}" Width="100" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,10,10" Click="CancelButton_Click"/>
        <Button Name="BackButton" Content="{DynamicResource Back}" Width="100" Height="25" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Click="BackButton_Click"/>
    </Grid>
</UserControl>
