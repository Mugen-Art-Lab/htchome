<Window x:Class="HTCHome.Manager.NvidiaCompatibilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NVIDIA Compatibility" Height="470" Width="920" MinHeight="420" MinWidth="860"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="14">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock x:Name="HeaderText" FontSize="22" FontWeight="SemiBold" />
            <TextBlock x:Name="DescriptionText" Margin="0,4,0,0" TextWrapping="Wrap" Opacity="0.75" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Border BorderBrush="#33000000" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock x:Name="ExclusionHeaderText" FontWeight="SemiBold" />
                        <TextBlock x:Name="ExclusionStatusText" Margin="0,3,0,0" TextWrapping="Wrap" />
                        <TextBlock x:Name="ExclusionNoteText" Margin="0,4,0,0" TextWrapping="Wrap" Opacity="0.65" />
                    </StackPanel>
                    <Button x:Name="ApplyExclusionsButton" Grid.Column="1" Width="175" Height="30" Margin="14,0,0,0" VerticalAlignment="Center" Click="ApplyExclusionsButton_Click" />
                </Grid>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock x:Name="UpdatedText" VerticalAlignment="Center" Opacity="0.65" />
                <Button x:Name="RefreshButton" Grid.Column="1" Width="110" Margin="0,0,8,0" Click="RefreshButton_Click" />
                <Button x:Name="CloseButton" Grid.Column="2" Width="100" Click="CloseButton_Click" />
            </Grid>
        </StackPanel>

        <Grid>
            <Border BorderBrush="#33000000" BorderThickness="1" CornerRadius="6" />
            <ListView x:Name="RowsList" Margin="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridViewColumn x:Name="ProfileColumn" Width="220">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding ProfileName}" HorizontalAlignment="Stretch" TextAlignment="Left" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="PID" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding PidText}" HorizontalAlignment="Stretch" TextAlignment="Center" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="NvidiaColumn" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding NvidiaText}" HorizontalAlignment="Stretch" TextAlignment="Center" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Handles" Width="105">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding HandlesText}" HorizontalAlignment="Stretch" TextAlignment="Center" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="DeltaColumn" Width="115">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding DeltaText}" HorizontalAlignment="Stretch" TextAlignment="Center" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="HealthColumn" Width="165">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding HealthText}" HorizontalAlignment="Stretch" TextAlignment="Center" /></DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
